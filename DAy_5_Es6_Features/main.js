import multiply, { divide } from './mathfunction.js'; // Note the .js

const result = multiply(20, 10);
const result1 = divide(20, 5);

console.log('The Multiplication result is', result); // 200
console.log('The Division result is', result1);      // 4
